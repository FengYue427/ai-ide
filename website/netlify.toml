# Legacy marketing-site config (static landing only).
# Production IDE + API: deploy the repo root to Vercel — see ../README.md

[build]
  publish = "website"
  command = ""

[build.environment]
  NODE_VERSION = "20"

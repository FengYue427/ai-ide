# v1.1 功能开关占位（规划）

> **用途**：v1.1 开发期默认关闭，避免半成品暴露。  
> **实现**：`import.meta.env.VITE_*` + 设置页「实验功能」区（可选）。

| 变量 | 默认 | 说明 |
|------|:----:|------|
| `VITE_BACKGROUND_AGENT` | `false` | 后台任务队列 UI + API |
| `VITE_COLLAB_M1_SIGNAL` | `false` | 稳定协作信令（替代纯 WebRTC Beta） |
| `VITE_AI_GATEWAY` | `false` | 平台额度转发（Pro+） |

---

## 验收（v1.1.0 时）

- 生产环境 **全部为 false**
- 文档列出每个开关的依赖与手测路径

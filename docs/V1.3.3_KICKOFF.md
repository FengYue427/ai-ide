# v1.3.3 Kickoff — Python + Agent 预算 + 大仓提示

> **更新**：2026-06-05  
> **类型**：patch · **v1.3.x 第 3 轮（共 9 轮至 1.3.9）**  
> **前置**：v1.3.2 GA · tag `v1.3.2`  
> **下一版**：[V1.3.4_KICKOFF.md](./V1.3.4_KICKOFF.md) 索引可观测

---

## 北极星

加固 v1.3.0 **F1（Python）** 与 **F5（Agent 索引）**，并补齐大仓 **capped** 用户提示。  
本 patch **不含** embedding 指标（→ 1.3.4）、Tab 深化（→ 1.3.5）。

---

## 任务（I1–I3）

| ID | 项 | 验收 |
|----|-----|------|
| **I1** | Python `from lib.util import greet` import 链 | F12 落 `lib/util.py` · 单测 ≥3 · E2E 扩展 |
| **I2** | Agent 索引上下文字符预算（~1200） | 大仓 Agent 不触发 payload 预检红条 · 单测 |
| **I3** | 索引 `capped` 设置文案 + 链 `BROWSER_LIMITATIONS#capacity-limits` | zh/en/ja |

**用户验收句**：Python 小仓 F12 跟手；开 Agent 时索引段不再把请求撑爆。

---

## 非目标

- Embedding hit/miss（1.3.4）· Tab 设置 UI（1.3.5）· Git（1.3.8）· v1.4 编码

---

## 门禁

```bash
npm run test:local && npm run test:e2e:local
```

Tag：`v1.3.3`

# 真正上市（GA）就绪评估

> **对象**：`v1.0.0-rc.1` 及 P3 完成后的代码库  
> **读者**：产品、研发、运维  
> **关联**：[DEPLOY_CHECKLIST.md](./DEPLOY_CHECKLIST.md)、[AUTH_BILLING_QA.md](./AUTH_BILLING_QA.md)

---

## 1. 上市档位定义

| 档位 | 含义 | 当前是否达标 |
|------|------|:------------:|
| **D0 演示站** | BYOK、无账号、纯前端 | ✅ 已长期具备 |
| **D1 技术预览** | 账号 API + 云工作区 + 配额 UI | 🔶 生产需 `deploy:check` 冒烟 |
| **D2 MLP（最小可上市）** | 稳定注册、云工作区、法务页、观测、公测配额 | 🔶 P0' 代码门禁 ✅；差法务审阅与 Sentry |
| **D3 可收款 GA** | D2 + 真实订阅与发票/退款流程 | ⬜ 差 P1 路径 B+ 商户 |
| **D4 企业级** | SSO、审计、专用部署、SLA | ⬜ 未规划 |

**结论（2026-05）**：产品处于 **D0～D1 之间**；对外可称 **RC / 公测**，不宜称 **正式 GA 或商业订阅上线**。

---

## 2. MLP（D2）必选项清单

### 2.1 基础设施与发布

| # | 项 | 状态 | 动作 |
|---|-----|------|------|
| L1 | Vercel `DATABASE_URL`、`AUTH_SECRET`、`APP_URL` 正确 | 🔶 用户已配 | 跑 `deploy:check` + 记录结果 |
| L2 | `/api/health` 返回预期 JSON | 🔶 | 生产冒烟 |
| L3 | `vercel.json` 不吞 `/api/*` | ✅ | 回归每次改路由 |
| L4 | Prisma migrate 与生产 schema 一致 | 🔶 | 发版前 `prisma migrate deploy` |
| L5 | 构建 + `npm test` CI 绿 | ✅ | 保持 |
| L6 | `test:integration:local` 绿 | ✅ | `npm run p0:gate` |

### 2.2 账号与安全

| # | 项 | 状态 | 动作 |
|---|-----|------|------|
| L7 | 注册 / 登录 / 登出 / 会话过期 | 🔶 | E2E + 人工 |
| L8 | 密码找回（邮件） | 🔶 骨架 | 生产 SMTP 或暂禁入口 |
| L9 | OAuth（若开启）回调 URL 与生产域一致 | 🔶 | OAUTH_SETUP 勾选 |
| L10 | API 限流、Cookie `HttpOnly`/`Secure` | ✅ 代码有 | 渗透抽查 |
| L11 | 无密钥进 Git；`git remote` 无嵌入 PAT | ⚠️ | **轮换 token，改 SSH/凭据管理器** |
| L12 | 隐私政策 / 服务条款 / 联系入口 | 🔶 模板页 | 法务审阅替换占位文案 |

### 2.3 产品与体验

| # | 项 | 状态 | 动作 |
|---|-----|------|------|
| L13 | 新用户：欢迎 → 模板 → 可编码 | ✅ | E2E 覆盖 |
| L14 | 云工作区创建 / 列表 / 切换 | 🔶 | 生产 DB 实测 |
| L15 | AI 配额耗尽提示与 429 处理 | ✅ | Chat + 设置已统一 |
| L16 | 离线 / API 失败降级文案 | 🔶 | 补全关键路径 toast |
| L17 | 浏览器限制说明（WebContainer、文件大小） | ✅ | `/help/browser-limits.html` + 欢迎页链接 |

### 2.4 商业化（MLP 可不收款）

| # | 项 | 状态 | 动作 |
|---|-----|------|------|
| L18 | 路径 A：公测不收款文案一致 | ✅ | `billingPath=A` + 订阅「公测免费」 |
| L19 | 路径 B：checkout → webhook → 订阅状态 | 🔶 | 骨架 + 集成测 simulate/order；生产需商户 |
| L20 | 退款 / 取消订阅 / 发票 | ⬜ | D3 |

### 2.5 观测与运维

| # | 项 | 状态 | 动作 |
|---|-----|------|------|
| L21 | 前端错误上报（Sentry 等） | ⬜ | P0' 或 P2 遗留 |
| L22 | 服务端日志与告警 | 🔶 | Vercel + Neon 控制台 |
| L23 | 备份与 RPO/RTO 说明（Neon） | ⬜ | 运维一页纸 |
| L24 |  incident 联系人与回滚流程 | ⬜ | 文档 + 标签发版 |

---

## 3. D3（可收款 GA）附加项

- 支付宝/微信 **生产** 或 Stripe **live** 打通且对账。
- `billingMode` 生产为 `live`，关闭 `dev_mock`。
- 订阅升降级、宽限期、欠费停服策略书面化。
- 国内 ICP/备案若主域名在国内运营（产品决策，非代码）。

---

## 4. 已知「上市 blocker」详解

### 4.1 浏览器 IDE 固有限制

- 无法提供与 VS Code 同等的 **全语言 LSP** 与 **原生调试器**。
- **WebContainer** 不支持任意 native 二进制；大仓库受内存与文件数限制。
- **对策**：诚实文档 + 定位「Web 优先场景」；长期可选 Electron（P4-7）。

### 4.2 插件安全

- 生产环境 `loadPlugin` 限制手动 JSON，降低 XSS/恶意代码风险。
- 官方目录 `installCatalogEntry` 为白名单；**尚无第三方审核与签名（M2）**。
- **对策**：GA 前市场仅官方包；开放上传需 M2 + 法律条款。

### 4.3 协作

- Yjs + WebRTC 为 **Beta**，无生产级信令与 SLA。
- **对策**：设置与面板已标注 Beta；GA 不宣传「团队实时协作」除非完成 P4-5。

### 4.4 AI 与合规

- BYOK：用户自行对厂商合规负责；平台不存模型密钥。
- 若未来做平台网关：需单独隐私影响评估与定价（见 OPTIMIZATION_PLAN §6.3）。

### 4.5 测试债务

- 本地 E2E 易因未启 preview 失败；以 **CI 为准**。
- 集成测试未绿则 **不能签 D2**。

---

## 5. 建议上市时间线（假设 1 名全职 + 兼职）

| 里程碑 | 前置 | 最早 |
|--------|------|------|
| **RC 公测公告** | 当前 + README 更新 | 即时 |
| **D2 MLP** | P0' 全绿 + L12 法务 | +3～4 周 |
| **D3 可收款** | P1 路径 B + 商户 | +6～10 周 |
| **D4 企业** | 单独立项 | 6 月+ |

---

## 6. 发版前 30 分钟检查（可复制）

```text
[ ] npm run test:local
[ ] npm run test:integration:local（有 DB 时）
[ ] npm run build && npm run test:smoke
[ ] 生产 health + 注册登录一条流
[ ] 公测横幅与配额显示正确
[ ] CHANGELOG / 版本号 bump
[ ] 无 .env 与 token 进 commit
```

---

## 7. 与本轮 P3 的关系

P3 交付（@ 上下文、协作 Beta 文案、插件市场骨架）**不解除**上述 L6/L19/L21 等 blocker；它们提升差异化叙事，但不单独构成 GA 门禁通过。

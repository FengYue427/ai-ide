# v1.2.4 F2 — Agent 上下文可靠性

> **状态**：✅ 完成（2026-06-04）

---

## 交付

| 项 | 说明 |
|----|------|
| **Payload 预算可视化** | 输入区实时进度条 · `estimateChatPayload` / `ChatPayloadBudgetMeter` · 82% 黄 / 超预算红 |
| **@mention 预检** | `runMentionPreflight`：未解析 · 超 6 个 · 符号重名 · 发送时 observability |
| **大仓降级提示** | `getLargeRepoContextHint`：索引 capped / 接近上限时在启用工作区或 @ 时提示 |

---

## 验证

```bash
npm run test:local
```

单测：`chatPayloadEstimate` · `mentionPreflight` · `largeRepoContextHint` · `payloadBudget`

---

## 已知限制

- 实时估算 **不含** 异步语义检索与 MCP 段落（偏保守下限）
- Agent 工具循环路径仍按实际 `aiMessages` 在发送时计量

---

## 参考

- `src/services/chatPayloadEstimate.ts`
- `src/services/mentionPreflight.ts`
- `src/services/largeRepoContextHint.ts`
- `src/components/ChatPayloadBudgetMeter.tsx`

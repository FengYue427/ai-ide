# v1.3.0 GA 执行清单

> **E2E/stack 热修与生产门禁已并入 [v1.3.1](./V1.3.1_GA_EXECUTION.md)**。建议以 tag `v1.3.1` 作为对外稳定点。

## 版本

- `package.json`: **1.3.0**（能力基线）
- Tag：建议跳过单独 `v1.3.0`，使用 **`v1.3.1`**

## F1–F7 交付摘要

| 阶段 | 交付物 | 验证 |
|------|--------|------|
| F1 | Python `def`/`class` 符号、跨文件定义/引用、E2E `py-cross-file-navigation` | `languageServiceHost.test.ts`、E2E |
| F2 | Embedding IndexedDB 持久化（7 天 TTL）、`indexBuildTelemetry` | `indexBuildTelemetry.test.ts`、`semanticSearchService` |
| F3 | `SettingsBackgroundAgentCard`、后台 Agent 特性只读 | `backgroundAgentFeatures` |
| F4 | `SettingsTabCompletionCard`、复用 `metricsDesc` | 设置页 `data-testid=settings-tab-completion` |
| F5 | `agentIndexContextSection` 注入 Agent 上下文 | `agentIndexContextSection.test.ts` |
| F6 | 插件审核 total/pending、`reviewsCount` i18n | `SettingsPluginOpsCard` |
| F7 | `v13Features`、设置卡、E2E `v13-features` | `v13Features.test.ts`、E2E |

## 本地验证（已通过）

```bash
npm run test:local
```

## GA 前建议

```bash
npm run test:e2e:local
# 重点: py-cross-file-navigation, v13-features, plugin-ops
```

## Git 发布（需显式执行）

```bash
git add -A
git commit -m "release: v1.3.0 — Python nav, index 2.0, agent context, settings cards"
git tag v1.3.0
git push origin main
git push origin v1.3.0
```

## GitHub Release

- 正文：`docs/RELEASE_NOTES_v1.3.0.md`
- 环境说明：`docs/V1.3_ENV.md`

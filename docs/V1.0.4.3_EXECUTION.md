# v1.0.4.3 执行清单（E3 检索 · @ 与语义）

> **总规划**：[V1.0.4.x_MASTER_PLAN.md](./V1.0.4.x_MASTER_PLAN.md)  
> **路线图**：[ROADMAP_V1.0.4.x.md](./ROADMAP_V1.0.4.x.md) §4  
> **语义规格**：[SEMANTIC_SEARCH_ONBOARDING.md](./SEMANTIC_SEARCH_ONBOARDING.md)  
> **前置**：建议 **`v1.0.4.2`** 已发（E1′+E2′ 深化）

---

## 0. DoD

| ID | 交付 | 通过标准 |
|----|------|----------|
| 4.3-1 | 语义 onboarding | 设置页无 Key / 未开启时有引导 |
| 4.3-2 | @ 首次引导 | ≥10 文件后 Chat 可 dismiss 提示 |
| 4.3-3 | 设置页「索引与 @」卡片 | 状态 + 重试 + 链到限制文档 |
| 4.3-4 | i18n 关键路径 | Agent/MCP/索引/语义 无漏翻 |
| 4.3-5 | `package.json` → **1.0.4.3** | spotcheck 自动项绿 |

**Could**：building 时 `@` 禁用（4.3-6）。

---

## 1. 代码落点（建议）

| 模块 | 路径 | 改动 |
|------|------|------|
| 语义引导 | `SettingsCenter.tsx` | onboarding 文案、Key 检测 |
| @ 引导 | `ChatPanel.tsx` | localStorage dismiss |
| 索引卡片 | `SettingsCenter.tsx` 或新 `IndexSettingsSection.tsx` | 复用 `projectIndexManager` |
| i18n | `translations.ts` | `settings.semantic.onboarding.*`、`settings.index.*` |

**已有（勿重做）**：

- `ChatPanel` 索引错误 + `chat.indexRetry`
- `semanticSearchPrefs` + `buildSemanticContextSection`

---

## 2. 版本与发包

1. `package.json` → **`1.0.4.3`**
2. `CHANGELOG.md` → `[1.0.4.3]`
3. 门禁：

```powershell
npm run test:local
npm run go-live:preflight
npm run rc:live-spotcheck
```

4. 部署；可与 **1.0.4.4** 合并 tag **`v1.0.4.4`**

---

## 3. 手测清单

- [ ] 导入 15 文件 → 等索引完成 → `@` 选文件 → 发送问题
- [ ] 关语义开关 vs 开语义（有 embedding Key）对比 system 差异
- [ ] 故意触发索引 error → Chat 重试 + 设置页重试均可用

---

## 4. 非目标

向量库服务 · 索引 5000 · 新搜索算法

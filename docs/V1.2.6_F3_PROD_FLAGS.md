# v1.2.6 F3 — 生产特性开关

## 默认

| 能力 | 生产默认 | 关闭方式 |
|------|:--------:|----------|
| 多根工作区 | 开 | `VITE_MULTI_ROOT=false` |
| 虚拟文件树 | 开（随 multi-root） | `VITE_VIRTUAL_FILE_TREE=false` |
| 插件可信市场 | 关 | `VITE_PLUGIN_TRUST_MARKET=true` 或 localStorage |

开发/测试运行时逻辑不变（`MODE=test` 仍为关）。

## 设置页

「功能」标签 — **v1.2 平台能力（只读）** 状态列表（`data-testid="settings-v12-features"`）。

## 运维对照

- 服务端 env：[VERCEL_V1.2_PRODUCTION_ENV.md](./VERCEL_V1.2_PRODUCTION_ENV.md)
- 设置 → **功能** → **插件运维（只读）**（v1.2.7）：health `plugins.*` · 发布审核列表

## E2E

- `e2e/v12-features.spec.ts`
- `e2e/plugin-ops.spec.ts`

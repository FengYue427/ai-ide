# v1.2.9 F2 — 动态 MCP payload 预留

## 目标

v1.2.8 固定 `CHAT_PAYLOAD_MCP_RESERVE_BYTES=6KB`；本阶段按已连接 MCP 工具数量动态估算（仍保守 capped）。

## 交付

- [x] `mcpPayloadReserve.ts` — base + per-tool，max 12KB，未知 count 回退 6KB
- [x] `refreshMcpToolCountEstimate` — 列出 enabled server 工具并缓存 localStorage
- [x] `estimateChatPayload({ mcpToolCount })` — Chat 表计联动
- [x] 单测：`mcpPayloadReserve` · `chatPayloadParity` 动态 reserve · `forceSlim` 跳过 mention 块

## 参考

- `src/services/mcpConfigService.ts`
- `src/services/chatSendPreflight.test.ts`

# v1.2.9 F1 — TS 引用行号精度

## 目标

v1.2.8 的 `ReferencesPeekBar` 使用正则 `goToReferences`，行号与 Monaco TS Worker / `registerEditorOpener` 不一致。

## 交付

- [x] `monacoLocationToReference` — Worker 坐标 → 工作区 path/line/column
- [x] `Editor.tsx` Shift+F12：优先 TS Worker 填充 Peek，回退正则
- [x] `registerEditorOpener`：跳转使用 `resolveReferenceNavigation` 保留列号
- [x] E2E：Peek 点击 `main.ts` 断言行 4（`greet()` 调用处）

## 验证

```bash
npm run test:local
npm run test:e2e:local -- e2e/ts-cross-file-navigation.spec.ts
```

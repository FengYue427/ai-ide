# v1.4.6 Kickoff — Tab++ 深化 I（FIM middle · P95/debounce UX）

> **更新**：2026-06-05  
> **类型**：patch · Tab/FIM 体验抛光  
> **状态**：✅  
> **前置**：v1.4.3 Tab++ spike · v1.4.5 hooks schema  
> **之后**：v1.4.7（Runtime 深化 I）

---

## 北极星

让 Tab 补全在**有选区**时使用 FIM **middle** 段上下文；POC 模式下在设置页展示更激进的 **P95 / debounce** 目标，便于实验与度量。

---

## 任务

| ID | 项 | 状态 |
|----|-----|:----:|
| **T1** | `fimMiddleSegment.ts` — prefix/middle/suffix 拆分 | ✅ |
| **T2** | `registerInlineCompletion` · `inlineCompletionService` 接入 middle | ✅ |
| **T3** | `inlineCompletionMetrics.fimMiddleContexts` 计数 | ✅ |
| **T4** | `getTabCompletionP95TargetMs()` — POC 400ms / 默认 800ms | ✅ |
| **T5** | POC debounce 默认 280ms（`inlineCompletionPrefs`） | ✅ |
| **T6** | `SettingsTabCompletionCard` POC 目标行 | ✅ |
| **T7** | 单测 · E2E POC 目标行 | ✅ |

---

## 验收

| # | 项 |
|---|-----|
| 1 | 非空选区时 FIM 请求携带 `middle`，prompt 含 replace 区域 |
| 2 | 指标快照含 `fimMiddleContexts` · `p95TargetMs` 随 POC 切换 |
| 3 | Tab++ POC 开启时设置卡显示 P95 &lt; 400ms · debounce 280ms |
| 4 | `test:local` 绿 · E2E 绿 |

---

## 禁止

- Tab++ **生产 env 默认全开**
- 部分接受快捷键（→ v1.5 F2）
- 宣传 · 上架

---

## 门禁

```bash
npm run test:local
npm run test:e2e:local
```

Tag：`v1.4.6`

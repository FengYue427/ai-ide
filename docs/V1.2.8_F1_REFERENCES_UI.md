# v1.2.8 F1 — 引用 Peek 与跨文件 E2E

## 目标

v1.2.7 已覆盖「转到定义」跨文件；本阶段补齐 **引用** 路径与结果 UI。

## 交付清单

- [ ] E2E：`Shift+F12` / 命令面板「转到引用」在 `E2E_NAV_FILES` 上至少 2 条结果
- [ ] Monaco `editor.action.goToReferences` / peek 与 `registerEditorOpener` 跨文件 tab 切换
- [ ] 可选：侧栏或 Peek 列表展示引用文件路径

## 非目标

- 独立 tsserver

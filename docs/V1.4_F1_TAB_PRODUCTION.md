# v1.4 F1 — Tab/FIM 生产级（P95 可测 · 默认策略）

> **状态**：✅ 已交付（v1.4.0 GA）  
> **归属**：[V1.4_KICKOFF.md](./V1.4_KICKOFF.md) F1  
> **目标**：把 Tab/FIM 从“可观测”推进到“可运营”

---

## 本轮交付

- `tabCompletionLatencyPercentile`：延迟样本 ring buffer 与 P50/P95 计算
- `inlineCompletionMetrics`：新增 `p50LatencyMs` / `p95LatencyMs` / `p95UnderTarget`
- `inlineCompletionPrefs`：`VITE_TAB_FIM_PRODUCTION` 开启时强制 Tab on，debounce 默认 `300ms`
- `v14Features` + `SettingsV14FeaturesCard`：展示 v1.4 F1 策略状态
- 设置页 Tab 指标补充 P95 行（目标 `<800ms`）

---

## 验收口径

| 项 | 目标 |
|----|------|
| 延迟统计 | 有样本时可见 P50/P95/样本数 |
| 生产策略 | `VITE_TAB_FIM_PRODUCTION=true` 时默认启用 Tab + 300ms |
| 指标目标 | `P95 < 800ms`（无样本时显示 unknown） |
| 自动化 | `test:local` 绿 |

---

## 后续（F1 剩余）

1. 增加 UI E2E：验证 Features 页 v1.4 卡片与指标行  
2. 增加 env 文档：`V1.4_ENV.md`（F1/F2/F3 逐步补齐）  
3. 收集真实 smoke 周样本，校准 `P95` 阈值与 debounce 默认值

# v1.5.1 + v1.5.2 GA 执行清单 — 生产部署 · Tab++ 默认策略

> **用途**：发版当日勾选 · **Patch 表**：[ROADMAP_V1.5.x_PATCHES.md](./ROADMAP_V1.5.x_PATCHES.md)  
> **环境**：[V1.5_ENV.md](./V1.5_ENV.md)

---

## 版本

| Patch | 北极星 | 本 repo |
|-------|--------|---------|
| **v1.5.1** | 线上与 repo 一致 | `.env.production` · `verify:env:v15` · health 1.5.x |
| **v1.5.2** | Tab 敢默认开 | prod build 默认开 v1.5 flags · P95 400ms · debounce 250ms |

- `package.json`: **1.5.2**
- Tag: `v1.5.2`（含 1.5.1 部署项）

---

## v1.5.1 交付

| # | 项 | 状态 |
|---|-----|:----:|
| P1 | `.env.production` — v1.5 Vite 全开 | ✅ |
| P2 | `scripts/verify-env.mjs --v15-production` | ✅ |
| P3 | `npm run verify:env:v15` | ✅ |
| P4 | `lib/api/releaseVersion.ts` FALLBACK → 1.5.2 | ✅ |
| P5 | `src/lib/v15ProductionEnv.ts` 部署 catalog | ✅ |
| P6 | 欢迎页 onboarding 去掉 BYOK 表述 | ✅ |
| P7 | `welcome-platform-cta` testid | ✅ |

### Vercel Production（手动）

```bash
npm run verify:env:v15
npm run smoke:production -- https://your-app.vercel.app
```

确认 `/api/health` 返回 `version` ≥ **1.5.2**。

---

## v1.5.2 交付

| # | 项 | 状态 |
|---|-----|:----:|
| T1 | `v15Features.ts` — prod build 默认开（`false` 可显式关） | ✅ |
| T2 | `getTabCompletionP95TargetMs()` — Tab++ 生产 **400ms** | ✅ |
| T3 | `SettingsTabCompletionCard` — P95 采集提示 | ✅ |
| T4 | 单测：`v15Features` · `tabCompletionLatencyPercentile` · `v15ProductionEnv` | ✅ |
| T5 | `V1.5_ENV.md` — prod 默认策略更新 | ✅ |

---

## 门禁

```bash
npm run test:unit          # ≥789
npm run verify:env:v15
npm run test:e2e:local     # tab-plus-plus.spec.ts
```

---

## 发版命令（可选）

```bash
git add -A
git commit -m "release: v1.5.2 — prod env defaults, Tab++ production GA"
git tag v1.5.2
git push origin main
git push origin v1.5.2
```

---

## 下一 patch

**v1.5.3** — Runtime 抛光（verify 文案 · queue UX · runtime-state 可读）

# v1.4.5 Kickoff — hooks.yaml schema · 设置预览

> **更新**：2026-06-05  
> **类型**：patch · Spec 工程（**仅 schema + 只读预览**）  
> **状态**：✅  
> **前置**：v1.4.4 AIDE Runtime ADR  
> **之后**：v1.4.6（Tab++ 深化 I）

---

## 北极星

让 Spec 目录中的 `hooks.yaml` **可解析、可校验、可在设置页只读预览**，为 v1.5 F4 hook 执行打底；**不执行**任何 hook。

---

## 任务

| ID | 项 | 状态 |
|----|-----|:----:|
| **H1** | `hooksSchema.ts` — 无外部 YAML 依赖的 v1 子集解析/校验 | ✅ |
| **H2** | `specHooksPreview.ts` — 从 workspace 文件构建预览 | ✅ |
| **H3** | `SpecCatalogSection` — hooks 只读预览块 + `data-testid` | ✅ |
| **H4** | `PanelHost` → `SettingsCenter` 透传 `specHooksPreviews` | ✅ |
| **H5** | i18n `spec.catalog.hooksPreview` | ✅ |
| **H6** | 单测 `hooksSchema` · `specHooksPreview` | ✅ |
| **H7** | E2E `runtime-hooks.spec.ts` | ✅ |

---

## 验收

| # | 项 |
|---|-----|
| 1 | `parseHooksYaml` 校验 version · on 四类 · run 三类 · 必填字段 |
| 2 | 设置 → Features → Spec 目录展示 hooks 摘要行（`id · on · run`） |
| 3 | 非法 YAML 在预览区展示错误（红色） |
| 4 | `test:local` 绿 · E2E hooks 预览可见 |

---

## 禁止

- `hookRunner` 生产执行（→ v1.5 F4）
- hooks 默认自动运行
- 宣传 · 上架

---

## 门禁

```bash
npm run test:local
npm run test:e2e:local
```

Tag：`v1.4.5`

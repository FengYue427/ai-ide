# v1.2.6 F2 — Agent 发送预检闭环

## 行为

- **@ 未解析**：非 `forceSlim` 时阻止发送并提示（`evaluateSendMentionGate`）。
- **Payload 表计**：`CHAT_PAYLOAD_METER_RESERVE_BYTES`（12KB）计入进度条，覆盖异步语义/MCP 未纳入 sync estimate 的缺口。
- **发送实测**：`comparePayloadEstimateToSend` 超 20% 漂移时 `chat.payload_estimate_drift` 事件。

## 代码

- `src/services/chatSendPreflight.ts`
- `src/components/ChatPanel.tsx`

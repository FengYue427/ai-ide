# v1.3 主规划 — 语言 · 索引 · Agent 生产化

> **状态**：📋 规划  
> **Kickoff**：[V1.3_KICKOFF.md](./V1.3_KICKOFF.md)  
> **不做（本包）**：SSH · 企业 SSO · 完整 VS Code 扩展市场 · DAP 全语言

---

## 0. 决策摘要

| 项 | 拍板 |
|----|------|
| **北极星** | 浏览器 IDE 在 **Python（或首门 LSP）** + **仓库检索** + **关页继续 Agent** 上达到可宣传的三合一演示 |
| **P0** | Python LSP 试点（或 Monaco 语言服务 worker 方案 ADR） |
| **P1** | `projectIndexManager` 增量 2.0 + embedding 持久化 |
| **P2** | 后台 Agent 生产默认策略 + E2E |
| **非目标** | Cursor Index 全量对标 · Devin 级长时自主 Agent |

---

## 1. 与 v1.2.x 边界

| v1.2.9 已交付 | v1.3 承接 |
|---------------|-----------|
| TS 引用 Peek 与 Worker 对齐 | F1 扩展到 Python |
| 动态 MCP payload 预留 | F2 Agent 上下文与索引联动 |
| 插件 publish 审核 API | 第三方插件案例（1.3.x patch） |
| 平台 AI 用量仪表盘 | Tab 补全与平台路由抛光（F4） |

---

## 2. F1 — Python / LSP 试点

| 项 | 说明 | 优先级 |
|----|------|:------:|
| ADR | 选型：Monaco 内置 vs `pyright` worker vs 代理 LSP | P0 |
| 导航 | `.py` 文件 F12 / 大纲至少一种可用 | P0 |
| E2E | 最小样本 `main.py` + `lib/util.py` 跨文件跳转 | P0 |
| 回退 | 无 LSP 时保留符号正则索引 | P1 |

**验收**：命令面板「转到定义」在 Python 样本上打开正确文件行。

---

## 3. F2 — 索引 2.0

| 项 | 说明 | 优先级 |
|----|------|:------:|
| 增量 | 单文件 patch 不触发全量防抖重建（已有 API 加固） | P0 |
| Embedding | BYOK embedding 结果 IndexedDB 缓存 + TTL | P1 |
| Agent | `largeRepoContextHint` 与 capped 策略文档化 | P1 |
| 指标 | 索引重建耗时 telemetry（已有 drift 模式可复用） | P2 |

**验收**：500 文件样本索引重建 < 可接受阈值（文档写明）；Chat `@` 检索命中稳定。

---

## 4. F3 — 后台 Agent GA

| 项 | 说明 | 优先级 |
|----|------|:------:|
| 开关 | 生产 `VITE_BACKGROUND_AGENT` 策略与 [V1.1_FEATURE_FLAGS.md](./V1.1_FEATURE_FLAGS.md) 对齐 | P0 |
| Cron | `/api/jobs/process` 运维清单 | P0 |
| E2E | 登录 → 提交后台任务 → 轮询完成（mock API 或 stack） | P1 |
| 演示 | 5 分钟脚本：注册 → Agent → 关页 → 回来看 Diff | P1 |

**验收**：Pro 用户可在 UI 看到后台任务 Tab 且任务状态终态可预览 Diff。

---

## 5. F4 — Tab 补全（可选 patch）

| 项 | 说明 |
|----|------|
| 延迟 | `inlineCompletionMetrics` 分位上报 |
| 路径 | FIM → 平台 AI 失败率降级策略 |

可下沉 **v1.3.1.x** 若 F1–F3 工期紧张。

---

## 6. 风险

| 风险 | 缓解 |
|------|------|
| 浏览器内 Python LSP 体积/性能 | 先单语言；桌面 Electron 作 Plan B |
| 后台 Agent 成本 | 配额 + Free 日限已在 entitlement |
| 范围蔓延 | 每阶段独立 tag 或 1.3.x patch，F4 可砍 |

---

## 7. 文档（随阶段创建）

| 文档 | 时机 |
|------|------|
| `ADR_V1.3_PYTHON_LSP.md` | F1 启动 |
| `V1.3_F1_*.md` | F1 开发 |
| `RELEASE_NOTES_v1.3.0.md` | GA |

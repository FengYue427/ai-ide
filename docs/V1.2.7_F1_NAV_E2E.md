# v1.2.7 F1 — 跨文件语义导航 E2E

## 目标

回归 v1.2.6 Monaco TS Worker 路径，避免仅测命令面板存在性。

## 交付清单

- [x] `E2E_NAV_FILES`：至少 `lib/greet.ts` + `main.ts`（import 调用）
- [x] `e2e/ts-cross-file-navigation.spec.ts`：光标在符号上 → 转到定义 → 活动 tab 切换
- [x] 命令面板转到定义 · 引用命令可见
- [x] `editorModelUri` / opener 单测覆盖 `file:///` 往返

## 非目标

- 独立 tsserver 子进程

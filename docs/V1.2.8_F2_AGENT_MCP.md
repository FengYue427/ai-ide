# v1.2.8 F2 — Agent MCP 与 @ 精简发送

## 目标

闭合 v1.2.7 F2 遗留：表计未覆盖 MCP 注入；重名 `@` 仅阻断、无 forceSlim 引导。

## 交付清单

- [x] `estimateChatPayload` / `buildPayloadMeterEstimate`：`CHAT_PAYLOAD_MCP_RESERVE_BYTES` + 表计脚注
- [x] 重名 `@`：预检 UI「精简发送」→ `handleSend(..., { forceSlim: true })`
- [x] 单测：`chatPayloadParity` MCP reserve + meter reserve

## 参考

- `src/services/chatSendPreflight.ts`
- `src/services/mcpAgentBridge.ts`

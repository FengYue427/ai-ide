# v1.2.8 F2 — Agent MCP 与 @ 精简发送

## 目标

闭合 v1.2.7 F2 遗留：表计未覆盖 MCP 注入；重名 `@` 仅阻断、无 forceSlim 引导。

## 交付清单

- [ ] `estimateChatPayload` / `buildPayloadMeterEstimate`：MCP 工具段保守字节（常量）
- [ ] 重名 `@`：预检 UI 提供「精简发送」入口（等同 `forceSlim`，跳过阻断）
- [ ] 单测：含 MCP reserve 的 meter ≥ sync send（代表性 fixture）

## 参考

- `src/services/chatSendPreflight.ts`
- `src/services/mcpAgentBridge.ts`

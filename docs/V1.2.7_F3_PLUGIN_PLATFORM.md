# v1.2.7 F3 — 插件生态 2.0 · 平台运维

## 目标

不扩营销，只把 **publish 审核** 与 **生产 env**、**平台 AI 配额可见性** 做到可运维。

## 交付清单

- [x] 设置「功能」页：`settings-plugin-ops` · health `plugins.*` 状态
- [x] 文档：`VERCEL_V1.2_PRODUCTION_ENV.md` ↔ `V1.2.6_F3` / `V1.2_ENV` 交叉链接
- [x] `GET /api/plugins/publish/reviews` + integration 冒烟 · 设置页只读列表
- [x] 平台用量：`quotaNearLimit` 黄条 · `platformProvider` 分桶

## 非目标

- 第三方插件商店公开上线
- 企业 SSO / 计费改造

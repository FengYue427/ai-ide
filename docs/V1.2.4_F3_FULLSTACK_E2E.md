# v1.2.4 F3 — 全栈 E2E 稳定

> **状态**：✅ 完成（2026-06-04）

---

## 交付

| 项 | 说明 |
|----|------|
| **fullstack helpers** | `e2e/fullstack-helpers.ts` · 复用 collab 注册/API 就绪 · i18n 选择器 |
| **用例** | API session 冒烟 · 注册 → 云端保存 → 列表带 Cloud 徽章 |
| **Playwright** | `fullstack` project：`workers: 1` · 120s timeout · serial |
| **脚本** | `scripts/run-e2e-stack.mjs` · `npm run test:e2e:stack` |
| **CI** | `e2e-stack` job 阻塞（已有 Postgres + db:seed） |

---

## 验证

```bash
npm run db:setup          # 本地首次
npm run test:e2e:stack
npm run test:local
```

---

## 参考

- `e2e/fullstack.spec.ts`
- `e2e/fullstack-helpers.ts`
- `.github/workflows/ci.yml` — `e2e-stack`

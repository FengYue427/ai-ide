# v1.2.8 F3 — 插件发布持久化与可信市场生产

## 目标

v1.2.7 审核队列为内存；本阶段可运维、可回归。

## 交付清单

- [x] Prisma `PluginPublishReview` + `POST` 写入 / `GET` 列表（DB 优先，内存回退）
- [x] 插件管理器 Manual：「提交审核」表单 → `reviewId`
- [x] [PLUGIN_TRUST_PRODUCTION.md](./PLUGIN_TRUST_PRODUCTION.md)
- [x] integration-api：reviews 列表 + 可选 `DATABASE_URL` 下 DB 行断言

## 非目标

- 公开第三方商店首页
- 自动合并到官方目录（仍人工）

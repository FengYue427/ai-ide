# v1.2.8 F3 — 插件发布持久化与可信市场生产

## 目标

v1.2.7 审核队列为内存；本阶段可运维、可回归。

## 交付清单

- [ ] Prisma `PluginPublishReview`（或等价表）+ `POST` 写入 / `GET` 列表
- [ ] 设置或插件市场：最小「提交审核」表单（manifest + source）→ 展示 `reviewId`
- [ ] `docs/PLUGIN_TRUST_PRODUCTION.md`：与 `VITE_PLUGIN_TRUST_MARKET` rollout 步骤
- [ ] integration-api：publish → DB 可读

## 非目标

- 公开第三方商店首页
- 自动合并到官方目录（仍人工）

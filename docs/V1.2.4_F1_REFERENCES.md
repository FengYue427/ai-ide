# v1.2.4 F1 — 转到引用 · 符号大纲

> **状态**：✅ 完成（2026-06-04）

---

## 交付

| 项 | 说明 |
|----|------|
| **转到引用** | `goToReferences` · `registerCrossFileReferenceProvider` 并入 `languageServiceHost` |
| **命令面板** | 「转到引用」· Shift+F12 · `requestGoToReferences` |
| **跨文件索引** | `referenceIndexService.findReferencesInFiles`（词边界 · 80 条上限） |
| **符号大纲** | 作用域前缀 `Class.method` · 嵌套项缩进 |

---

## 验证

```bash
npm run test:local
```

单测：`languageServiceHost.test.ts` · `referenceIndexService.test.ts` · `outlineDisplay.test.ts`

---

## 已知限制

- 引用搜索为 **词边界正则**，非 TS 语义分析；与 v1.2.1 跳定义同级精度
- Monaco peek 引用 UI 依赖内置 `editor.action.goToReferences`

---

## 参考

- `src/editor/languageServiceHostCore.ts`
- `src/editor/registerCrossFileReferences.ts`
- `src/lib/outlineDisplay.ts`
- `src/components/SymbolOutline.tsx`

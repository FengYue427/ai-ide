# v1.2.7 F2 — Payload 表计与发送对齐

## 目标

缩小 v1.2.6 表计（sync + 12KB reserve）与 `buildPromptWithSharedContext`（异步语义/MCP）之间的盲区。

## 交付清单

- [ ] `estimateChatPayload`：语义开启时加保守 token/字节上限
- [ ] Agent tool loop：表计标注「工具轮次未计入」或按轮次上限估算
- [ ] `@` 重名：发送前确认或默认阻断（与未解析区分）
- [ ] 单测：`comparePayloadEstimateToSend` 在 representative fixtures 内

## 参考

- `src/services/chatSendPreflight.ts`
- `src/services/chatPromptPipeline.ts`

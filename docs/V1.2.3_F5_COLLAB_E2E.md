# v1.2.3 F5 — 协作 E2E 稳定

## 目标

- 双浏览器 Playwright smoke 在 CI **必过**（不再 `continue-on-error`）
- 信令断开后 **30s 内重连** 逻辑有单测覆盖
- E2E helper 与 UI E2E 对齐（关闭登录遮罩、autosave 种子）

## 交付

| 项 | 说明 |
|----|------|
| `e2e/collab-smoke.spec.ts` | 串行执行 · Host 创建 → Viewer 只读 |
| `e2e/collab-helpers.ts` | `dismissAuthModalIfOpen` · 信令徽章文案 |
| `collaborationService.test.ts` | `scheduleReconnect` / `tryReconnect` |
| `playwright.config.ts` | `collab` project `workers: 1` |
| `.github/workflows/ci.yml` | `e2e-collab` 阻塞主 CI |

## 验证

```bash
npx prisma db push && npm run db:seed
npm run test:e2e:collab
npm run test:local
```

API 段：`npm run test:integration:local`（含 collab 房间 CRUD）

## v1.2.3 收口

F1–F5 完成后，工程线可打 **v1.2.3** tag；宣传/上架仍搁置。

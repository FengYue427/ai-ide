# v1.3 F2 — 索引 2.0

- [x] `embeddingPersistCache.ts` — 7 天 TTL IndexedDB
- [x] `semanticSearchService` 读写持久化层
- [x] `indexBuildTelemetry` — full / incremental / worker
- [x] `projectIndexManager` 记录耗时
- [x] 设置页索引状态展示 telemetry

# v1.3 F3 — 后台 Agent GA（运维可见）

- [x] `getBackgroundAgentFeatureStatus()` — cron 提示
- [x] `SettingsBackgroundAgentCard` — 设置 → 功能
- [x] 生产仍默认 `VITE_BACKGROUND_AGENT=false`

服务端：`BACKGROUND_AGENT_API_KEY` · Cron `GET /api/jobs/process`

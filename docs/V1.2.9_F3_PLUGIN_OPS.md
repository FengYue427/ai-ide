# v1.2.9 F3 — 插件审核运维

## 目标

v1.2.8 publish DB 已落地；本阶段补齐只读查询、列表筛选与 E2E smoke。

## 交付

- [x] `GET /api/plugins/publish/reviews?status=pending` — 状态筛选
- [x] `GET /api/plugins/publish/reviews/:reviewId` — 只读单条（当前用户）
- [x] 设置页 `settings-plugin-ops-status-filter` — 全部 / 待审核
- [x] E2E：`plugin-ops.spec` 筛选 + 插件管理器 Manual 提交表单 smoke

## 验证

```bash
npm run test:local
npm run test:e2e:local -- e2e/plugin-ops.spec.ts
npm run test:integration:local   # publish + reviews（需 DATABASE_URL）
```
